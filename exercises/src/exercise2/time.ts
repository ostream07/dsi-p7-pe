/* eslint-disable max-len */
/*
import {isConvertible} from './isConvertible';

export enum MagnitudeUnits {
  seg = 1,
  min = 60,
  h = 3600,
  day = 86400,
  week = 604800,
  month = 2592000,
  year = 31104000,

};

export class Time implements isConvertible<[MagnitudeUnits, number, MagnitudeUnits]> {
  constructor() {}
  static conversion(data: [MagnitudeUnits, number, MagnitudeUnits]): number {
    const value: number = data[1];
    let result: number = 0;
    if (data[0] < data[2]) {
      result = data[0] * value;
    }
    return result;
  }
}
*/
